const express = require('express');
const router = express.Router();
const Area = require('../models/Area');

// Add route to save area data
// Add route to fetch saved areas and display on the map

module.exports = router;
